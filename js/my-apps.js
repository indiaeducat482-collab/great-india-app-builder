import {auth,db} from "./firebase-config.js";
import {onAuthStateChanged,signOut} from "https://www.gstatic.com/firebasejs/12.2.1/firebase-auth.js";
import {collection,query,where,getDocs,deleteDoc,doc} from "https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js";
const $=s=>document.querySelector(s);$("#logoutBtn")?.addEventListener("click",async()=>{await signOut(auth);location.href="./login.html"});
onAuthStateChanged(auth,async u=>{if(!u){location.href="./login.html";return}try{const s=await getDocs(query(collection(db,"apps"),where("userId","==",u.uid)));const g=$("#appsGrid");if(s.empty){g.innerHTML='<div class="empty-state"><strong>You haven\'t created any apps yet.</strong><a class="btn btn-primary" href="./create-app.html">Create Your First App</a></div>';return}g.innerHTML=[...s.docs].map(x=>{const a=x.data();return `<article class="app-card"><div class="app-card-top"><div class="app-logo">${esc((a.appName||"GI").slice(0,2).toUpperCase())}</div><span class="status-badge ${a.status||"draft"}">${a.status||"draft"}</span></div><h3>${esc(a.appName)}</h3><p>${esc(a.websiteUrl)}</p><div class="card-actions"><a class="mini-btn" href="./app-details.html?id=${x.id}">OPEN</a><a class="mini-btn primary" href="./create-app.html?id=${x.id}">EDIT</a><a class="mini-btn" href="./app-details.html?id=${x.id}&build=1">BUILD</a></div></article>`}).join("")}catch(e){console.error(e);$("#appsGrid").innerHTML='<div class="empty-state">Could not load apps.</div>'}});
function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}