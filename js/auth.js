import{auth,db}from"./firebase-config.js";import{signInWithEmailAndPassword,sendPasswordResetEmail,createUserWithEmailAndPassword,updateProfile,signOut}from"https://www.gstatic.com/firebasejs/12.2.1/firebase-auth.js";import{doc,setDoc,getDoc,serverTimestamp}from"https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js";
const $=id=>document.getElementById(id);
export const getFriendlyError=e=>({ "auth/invalid-credential":"Email or password is incorrect.","auth/email-already-in-use":"This email is already registered.","auth/weak-password":"Password must be at least 6 characters.","auth/invalid-email":"Enter a valid email address."}[e.code]||e.message||"Authentication failed.");
export async function loginUser(email,password){const c=await signInWithEmailAndPassword(auth,email,password);const s=await getDoc(doc(db,"users",c.user.uid));if(s.exists()&&s.data().blocked===true){await signOut(auth);throw Error("Your account is blocked by admin.")}return c.user}
export async function resetPassword(email){return sendPasswordResetEmail(auth,email)}
export async function registerUser(name,email,password){const c=await createUserWithEmailAndPassword(auth,email,password);await updateProfile(c.user,{displayName:name});await setDoc(doc(db,"users",c.user.uid),{uid:c.user.uid,name,email,blocked:false,buildCount:0,createdAt:serverTimestamp(),updatedAt:serverTimestamp()},{merge:true});return c.user}
if($("login"))$("login").onclick=async()=>{try{await loginUser($("email").value.trim(),$("password").value);location.href="index.html"}catch(e){$("msg").textContent="❌ "+getFriendlyError(e)}};
if($("forgot"))$("forgot").onclick=async()=>{try{await resetPassword($("email").value.trim());$("msg").textContent="✅ Password reset email sent."}catch(e){$("msg").textContent="❌ "+getFriendlyError(e)}};
if($("register"))$("register").onclick=async()=>{try{const n=$("name").value.trim(),e=$("email").value.trim(),p=$("password").value,c=$("confirm").value;if(!n)throw Error("Name required");if(p!==c)throw Error("Passwords do not match");await registerUser(n,e,p);location.href="index.html"}catch(e){$("msg").textContent="❌ "+getFriendlyError(e)}};