import {auth,db} from "./firebase-config.js";
import {onAuthStateChanged,signOut} from "https://www.gstatic.com/firebasejs/12.2.1/firebase-auth.js";
import {collection,query,where,getDocs,orderBy,limit,getDoc,doc} from "https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js";
const $=s=>document.querySelector(s); $("#logoutBtn")?.addEventListener("click",async()=>{await signOut(auth);location.href="./login.html"});
onAuthStateChanged(auth,async u=>{if(!u){location.href="./login.html";return}const name=u.displayName||u.email.split("@")[0];$("#userName").textContent=name;$("#topName").textContent=name;$("#topEmail").textContent=u.email;$("#profileName").textContent=name;$("#profileEmail").textContent=u.email;$("#profileUid").textContent=u.uid;try{const q=query(collection(db,"apps"),where("userId","==",u.uid),orderBy("createdAt","desc"),limit(5));const s=await getDocs(q);let total=0,b=0,r=0;s.forEach(x=>{total++;const st=x.data().status;if(st==="building")b++;if(st==="ready")r++});$("#totalApps").textContent=total;$("#buildingApps").textContent=b;$("#readyApps").textContent=r;$("#recentApps").innerHTML=s.empty?'<div class="empty-state">No apps yet. Create your first app.</div>':[...s.docs].map(x=>{const a=x.data();return `<div class="app-row"><div><h3>${esc(a.appName)}</h3><p>${esc(a.websiteUrl)}</p></div><div class="app-actions"><span class="status-badge ${a.status||"draft"}">${a.status||"draft"}</span><a class="mini-btn primary" href="./app-details.html?id=${x.id}">Open</a></div></div>`}).join("")}catch(e){console.error(e);$("#recentApps").innerHTML='<div class="empty-state">Could not load apps. Check Firestore rules/index.</div>'}});
function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}