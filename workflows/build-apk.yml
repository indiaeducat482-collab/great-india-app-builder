name: Build Android APK
on:
  workflow_dispatch:
    inputs:
      website_url:
        description: Website URL to package
        required: true
        type: string
      app_name:
        description: Application name
        required: true
        type: string
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Prepare Android project
        run: |
          echo "Build pipeline placeholder"
          echo "Website: ${{ inputs.website_url }}"
          echo "App: ${{ inputs.app_name }}"
      - name: Build status
        run: echo "Android build workflow is configured. Add the Android project/build service for production APK generation."
